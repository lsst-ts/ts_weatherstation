title: Environment v1
description: Schema for Environment configuration files
type: object
properties:
  settingVersions:
    recommendedSettingsVersion: 'default'
    recommendedSettingsLabels: ['default', 'simulation']
settingsToApply:
setting:
  default:
    type: 'lsst'
    configuration:
      host: '10.0.2.15'  # IP of the weather station
      port: 50000
      buffer_size: 2048
      simulation: False
  simulation:
    type: 'lsst'
    configuration:
      host: 'localhost'  # IP of the weather station
      port: 50000
      buffer_size: 2048
      simulation: True
